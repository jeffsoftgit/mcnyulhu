angular.module('app').controller('app_viewissue', app_viewissue);
function app_viewissue($scope, app) {
    'use strict';
    app.init($scope)
    {
        /*var data=$scope.data;
        for(var i=0;i<data.issuelist.length;i++)
        {
            data.issuelist[i].summary='this me';
        }*/
    }
}